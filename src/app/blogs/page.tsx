import React from "react";

const page = () => {
  return (
    <div>
      <h1>
        This is the section where all the details of the events happened on the
        temple will be displayed
      </h1>
    </div>
  );
};

export default page;
